let user = {name:"Bhavy",gender:"M"};

console.log("Bhavy");
console.log(user.name);

// types : numbers, strings, boolean, null undefined, void, obj, array, tuples, ... , any(not recommanded), never, unknown